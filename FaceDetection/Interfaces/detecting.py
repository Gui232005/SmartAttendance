#Here we will display while the process is ongoing
from tkinter import *

root = Tk()

root.title("Smart Attendance")

root.attributes('-fullscreen', True)
root.title("Smart Attendance")

label = Label(root, text="A processar...", font=("Helvetica", 48))
label.pack(expand=True)
root.mainloop()

